# Plan: Firmware Size Optimization

## User Decisions

| Система | Решение |
|---------|---------|
| Ядро (system.cpp) | Упростить — убрать лишнюю инициализацию |
| RC ввод | Оставить всё |
| GCS/MAVLink | Убрать пустое/мёртвое (PID_TUNING, VFR_HUD) |
| Failsafe | Оставить всё |
| Арминг | Оставить всё |
| Логирование | Оставить MOTORI/MOTORO |
| Параметры | Убрать лишние k_param_* enum |

---

## Changes

### 1. system.cpp — упрощение

Убрать лишние вызовы из `init_ardupilot()`:
- `battery.init()` — нет мониторинга батареи
- `barometer.init()` / `barometer.calibrate()` — нет барометра
- `startup_INS_ground()` — убрать (AHRS/INS не нужны для manual)
- `ins.set_log_raw_bit()` — нет логирования IMU

Оставить:
- `notify.init()` — индикация
- `gcs().setup_uarts()` — MAVLink
- `init_rc_in()` — RC ввод
- `allocate_motors()` — моторы
- `rc().convert_options()` / `rc().init()` — RC каналы
- `init_rc_out()` — вывод
- Failsafe timer register
- Mode switch

### 2. GCS_Mavlink.cpp — убрать пустое

- `send_pid_tuning()` — уже no-op, убрать объявление и вызов
- `vfr_hud_alt()` — возвращает 0, упростить
- `vfr_hud_airspeed()` — возвращает 0, убрать
- `send_wind()` — пустой, убрать
- `high_latency_wind_*` — пустые, убрать

### 3. Parameters.h — убрать лишние k_param_*

Удалить enum values для:
- `k_param_simple_mode`, `k_param_dis_mask`, `k_param_pid_dz`
- `k_param_gcs_pid_mask`
- `k_param_rtl_alt_type`, `k_param_rtl_speed_cms`, `k_param_fs_batt_curr_rtl`, `k_param_rtl_cone_slope`
- `k_param_parachute`, `k_param_landinggear`, `k_param_avoid`, `k_param_avoidance_adsb`
- `k_param_throw_motor_start`
- Все k_param_* которые не используются

### 4. Blimp.h — убрать лишние declarations

- Убрать `rotate_NE_to_BF`, `rotate_BF_to_NE` (не используются)
- Убрать `convert_pid_parameters`, `convert_lgr_parameters`, `convert_fs_options_params`
- Убрать `update_altitude`, `update_batt_compass` (уже удалены из .cpp)
- Убрать `three_hz_loop`, `one_hz_loop`, `full_rate_logging`, `ten_hz_logging_loop`, `twentyfive_hz_logging`
- Убрать `read_AHRS`
- Убрать `fence_check`, `landinggear_update`
- Убрать `Log_Write_Attitude`, `Log_Write_EKF_POS`

### 5. Blimp.cpp — упрощение scheduler

- Убрать `three_hz_loop`, `one_hz_loop` из scheduler если они пустые
- Убрать `full_rate_logging`, `ten_hz_logging_loop`, `twentyfive_hz_logging` если удалены

## Files to Modify

| Файл | Изменение |
|------|-----------|
| `system.cpp` | Убрать инициализацию датчиков |
| `GCS_Mavlink.cpp` | Убрать пустые функции |
| `GCS_Mavlink.h` | Убрать declarations удалённых функций |
| `Parameters.h` | Убрать лишние k_param_* enum |
| `Blimp.h` | Убрать declarations удалённых функций |
| `Blimp.cpp` | Убрать пустые scheduler tasks |
