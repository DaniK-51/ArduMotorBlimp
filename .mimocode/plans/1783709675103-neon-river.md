# Plan: Full Vehicle Audit and Cleanup

## Issues Found

### Critical (won't compile)

1. **events.cpp**: `Failsafe_Action_Land` → `Failsafe_Action_BRAKE`
2. **events.cpp**: `flightmode->is_landing()` → remove (not in Mode class)
3. **events.cpp**: `gpsglitch_check()` uses `inertial_nav` → delete function
4. **events.cpp**: `sysid_myggcs_last_seen_time_ms` typo → `sysid_mygcs_last_seen_time_ms`
5. **inertia.cpp**: uses removed variables → delete file
6. **sensors.cpp**: uses removed variables → delete file
7. **ekf_check.cpp**: uses removed variables → delete file
8. **commands.cpp**: uses `ahrs` → simplify or delete

### Files to delete

- `inertia.cpp` — not needed for manual-only
- `sensors.cpp` — not needed for manual-only
- `ekf_check.cpp` — not needed for manual-only
- `commands.cpp` — not needed for manual-only

### defines.h cleanup

- Remove RTL/SmartRTL/Mission failsafe values
- Rename `FS_THR_ENABLED_ALWAYS_LAND` → `FS_THR_ENABLED_ALWAYS_BRAKE`
- Clean up unused `MASK_LOG_*` and `LOG_*_MSG` entries

### Parameters.cpp cleanup

- Default `FS_THR_ENABLE` = `FS_THR_ENABLED_ALWAYS_BRAKE` (was RTL)

### events.cpp rewrite

Simplify to only handle:
- Radio failsafe → BRAKE
- Battery failsafe → BRAKE
- GCS failsafe → BRAKE or disarm

### RC_Channel.h

- Fix comment referencing MotorMix.h

## Files to Modify

| File | Action |
|------|--------|
| `events.cpp` | Rewrite failsafe handlers |
| `defines.h` | Clean up constants |
| `Parameters.cpp` | Fix defaults |
| `RC_Channel.h` | Fix comment |
| `Blimp.h` | Remove declarations for deleted files |
| `inertia.cpp` | DELETE |
| `sensors.cpp` | DELETE |
| `ekf_check.cpp` | DELETE |
| `commands.cpp` | DELETE |
